# TODO

## Next Work

- Add Desktop PowerShell-style FTP checks, such as `Test-NetConnection <server> -Port 21` evidence plus a lightweight `ftp`/download verification path in reports.
- Add Desktop PowerShell-style TFTP/SYSLOG helpers if they can reuse existing `tftp` and `syslog` simulation flows without expanding state shape.
- Add report/export rows for Desktop command evidence generated by `Test-Connection`, `Test-NetConnection`, `Invoke-WebRequest`, `Send-MailMessage`, `Resolve-DnsName`, and listener/process commands.
- Consider extracting repeated Desktop service-command handling in `Editor.tsx` into shared helpers once the command surface settles.
- Add browser/visual regression coverage for the Desktop command prompt and Simulation dock when Playwright dependencies are available.

## Validation To Keep Running

- `npm run build`
- `npm run smoke:features`
- `npm run smoke:all`
- `git diff --check`

## Known Environment Gaps

- `npm run smoke:visual` is blocked by missing Playwright Chromium dependency `libnspr4.so`.
- `npm run build:go` is blocked because `go` is not installed.
- `npm run build:rust` is blocked because `wasm-pack` is not installed.

## Current Direction

- The recent work focused on Desktop/Command Prompt parity for Windows and PowerShell diagnostics.
- Keep future changes small, testable, and tied to existing simulator state rather than introducing new runtime state unless necessary.
