# 개발일지2

## 2026-06-18 09:29 KST
- 요구사항3 재확인: 디자인이 이전보다 나빠짐, 노드 드래그 불가, UI 돌출/깨짐, CLI가 하단/우측에 길게 내려감, Packet Tracer처럼 별도 창 필요, 우클릭 기본 브라우저 메뉴 대신 사이트 메뉴 필요, 선/포트 표시 필요, Packet Tracer 6.1 `.pkt` 호환성 혼동.
- Cisco Packet Tracer UI 조사:
  - NetAcad 도움말 기준 Packet Tracer는 상단 메뉴/툴바, Logical/Physical 워크스페이스 탭, 중앙 Workspace, Realtime/Simulation 바, 하단 Network Component Box(Device-Type Selection Box + Device-Specific Selection Box) 구조.
  - Logical Workspace에서는 장비 타입 선택 후 모델 선택, 워크스페이스 클릭 또는 드래그앤드롭으로 장비 배치.
  - Select Tool 상태에서 장비는 좌클릭 드래그로 이동.
  - 장비 클릭 시 Physical/Config/CLI/Desktop/Services 형태의 별도 설정 창이 뜸.
  - 연결은 Connections에서 케이블 선택 후 장비/인터페이스를 순서대로 선택하며, 케이블과 양끝 링크 라이트가 표시됨.
- GitHub 확인:
  - `gh repo list`에서 원격 저장소 `deploy103/Network-Editor-Web`, 기본 브랜치 `main` 확인.
  - 원격 커밋은 `f66fea0 Initial public release` 1개.
  - `/tmp/network-editor-web-main`에 main 클론 후 이전 디자인 확인: 중앙 워크스페이스, 하단 장비 팔레트 타일, 링크 라이트/링크 라벨, 별도 Inspector/CLI 구성 감각을 현 코드에 반영 중.
- 현재 개발 진행:
  - `Editor.tsx`에 Packet Tracer식 레이아웃 전환 작업 시작.
  - 장비 클릭 시 우측 고정 패널 대신 별도 `DeviceWindow`가 뜨도록 상태 추가.
  - 우클릭 메뉴 `Open`, `CLI`, `Desktop`, `Rename`, `Power`, `Delete` 추가 작업 중.
  - 노드 드래그가 끊기는 원인인 pointer capture/handler 위치를 수정 중.
  - 마우스 휠 기본 줌 동작으로 변경 중.
- 다음 작업:
  - CSS를 Packet Tracer식 상단/중앙/하단 구조로 재작성.
  - TypeScript 빌드 오류 수정.
  - 브라우저에서 드래그, 우클릭, 장비 창, CLI 창, 링크 표시 확인.

## 2026-06-18 09:34 KST
- 구현 완료:
  - 에디터 레이아웃을 기존 `좌 팔레트 / 중앙 / 우 검사패널`에서 `상단 메뉴/툴바 / 중앙 Workspace / 하단 장비·연결·시뮬레이션 트레이 / 별도 장비 창` 구조로 변경.
  - Logical/Physical 탭, Realtime/Simulation 탭, zoom HUD, cable HUD 추가.
  - 장비 클릭 시 `DeviceWindow` 별도 창으로 Physical/Config/CLI/Desktop/Services가 열리도록 변경.
  - 장비 우클릭 시 브라우저 기본 메뉴 대신 `Open`, `CLI`, `Desktop`, `Rename`, `Power`, `Delete` 사이트 메뉴 표시.
  - 노드 pointer capture 문제 수정: 노드 자체에 pointer move/up/cancel을 붙여 드래그 이동이 끊기지 않게 변경.
  - 마우스 휠 기본 동작을 줌으로 변경.
  - 팔레트를 Packet Tracer 하단 Network Component Box처럼 장비 타입 아이콘 + 모델 타일 + Connections 영역으로 재구성.
  - 모델 타일 드래그앤드롭 배치 지원.
  - 링크가 노드 중심이 아니라 장비 가장자리에서 시작/끝나도록 계산 변경.
  - 링크 라인 hitbox와 양 끝 링크 라이트 추가.
  - CLI/장비 설정은 별도 창 내부에서 스크롤되도록 CSS 조정.
  - Cisco Packet Tracer 6.1에서 열리지 않는 자체 PTWEB 파일을 `.pkt`로 내보내던 UX 오류 수정: 새 내보내기는 `.ptweb` 확장자로 변경.
  - README의 PTWEB `.pkt` export 문구를 `.ptweb` export로 수정.
- 검증:
  - `npm run build --workspace web` 통과.
  - Vite dev server 실행: `http://localhost:5173/`.
  - `curl -I http://localhost:5173/` 200 OK 확인.
- 제한:
  - 현재 환경에 Playwright/Chromium이 없어 실제 브라우저 스크린샷 검증은 아직 못 함.
- 다음 작업:
  - CLI 창 명령 도움말/힌트 개선.
  - 우클릭 메뉴 안정성, CSS overflow, 하단 패널 밀림 여부를 코드 기준으로 추가 점검.

## 2026-06-18 09:40 KST
- 추가 구현:
  - CLI 창 상단에 터미널 헤더와 명령 도움말 버튼 추가.
  - CLI 도움말 패널에서 명령 검색 및 클릭 입력 가능하도록 구현.
  - CLI 도움말이 열려도 터미널 출력/입력 영역이 창 밖으로 밀리지 않도록 grid rows 분리.
  - 빈 워크스페이스 우클릭 시 Chrome 기본 메뉴가 뜨지 않도록 차단하고 상태 메시지 표시.
  - 링크(케이블) 우클릭 메뉴 추가: 케이블 타입, 양끝 포트 라벨, status 표시, Remove Cable 가능.
- 검증:
  - `npm run build --workspace web` 재통과.
- 다음 작업:
  - 장비 창/하단 트레이 overflow 점검.
  - Packet Tracer식 장비 선택/연결 모드가 실제 코드 흐름과 어긋나는 부분 추가 수정.

## 2026-06-18 09:45 KST
- 기존 `alternative/조사.txt` 재확인:
  - Packet Tracer UI 구성 요소: 메뉴/툴바, Logical/Physical Workspace, Realtime/Simulation 바, 장비 선택 박스, 사용자 생성 패킷 창.
  - `.pkt`는 JSON/XML/ZIP이 아닌 proprietary binary로 보이며, 공개 문서/SDK 없이는 완전 호환 exporter 구현이 어렵다는 기존 결론 확인.
  - 기존 조사 원칙인 “`.pkt` 완전 호환 한계는 UI에서 명확히 표시”에 맞춰, 자체 포맷을 `.pkt`로 저장하던 부분을 `.ptweb`로 바꾼 결정 유지.
- 추가 구현:
  - 장비 설정 창을 고정 중앙 모달이 아니라 드래그 가능한 floating window로 변경.
  - 창 헤더 드래그로 위치 이동 가능, 화면 밖으로 나가지 않도록 clamp 처리.
- 검증:
  - `npm run build --workspace web` 통과.

## 2026-06-18 09:48 KST
- 추가 구현:
  - Rename 동작에서 브라우저 기본 `window.prompt` 제거.
  - 앱 내부 커스텀 `Rename Device` 다이얼로그 추가.
  - 우클릭 메뉴 → Rename → 앱 다이얼로그 → 적용/취소 흐름으로 변경.
- 검증:
  - `npm run build --workspace web` 통과.

## 2026-06-18 09:49 KST
- 추가 구현:
  - Simulation Events 영역을 카드 나열에서 Packet Tracer Event List에 가까운 표 형태로 변경.
  - 열 구성: Time, Last Device, At Device, Type, Info, Status.
  - 하단 패널 안에서 표가 조밀하게 표시되도록 sticky header와 ellipsis 처리 추가.
- 검증:
  - `npm run build --workspace web` 통과.
  - Vite dev server HTML 응답 확인.

## 2026-06-18 09:57 KST
- 추가 구현:
  - PDU 봉투 애니메이션 좌표를 예전 고정 중심값에서 새 장비 크기 기반 `nodeCenter` 계산으로 변경.
  - 하단 팔레트 클릭 시 열린 디바이스/링크 컨텍스트 메뉴가 남지 않도록 명시적으로 닫는 처리 추가.
  - 장비 창 탭 라벨을 `physical/config/cli`에서 `Physical/Config/CLI/Desktop/Services`로 변경.
  - Console 링크 라이트는 검정, blocked 링크 라이트는 amber로 표시되도록 Packet Tracer식 색상 보정.
  - Rename 다이얼로그는 Escape뿐 아니라 배경 클릭으로도 취소되도록 보정.
  - `alternative/조사.txt`의 오래된 `.pkt` 자체 export 계획 문구를 `.ptweb` 자체 포맷 export 기준으로 정정.
- 검증:
  - `npm run build --workspace web` 통과.
  - dev server 계속 실행 중: `http://localhost:5173/`.
- 다음 작업:
  - 실제 사용 흐름 기준으로 장비 배치/연결/창 조작의 남은 edge case 추가 점검.

## 2026-06-18 09:59 KST
- 검증 시도:
  - `npx --yes playwright --version` 결과: Playwright CLI 1.61.0 사용 가능.
  - `npx --yes playwright screenshot --viewport-size=1440,900 http://localhost:5173/ /tmp/network-editor-home.png` 실행 시도.
  - 실패 원인: Chromium headless 실행에 필요한 시스템 라이브러리 `libnspr4.so`가 없어 브라우저가 실행되지 않음.
- 현재 검증 기준:
  - TypeScript/Vite production build 통과.
  - Vite dev server 실행 유지.
  - `curl` 기반 HTML 응답 확인.
- 계속 진행:
  - 브라우저 스크린샷 검증은 시스템 라이브러리 설치가 필요하므로 보류.
  - 코드/빌드 기준으로 요구사항3의 상호작용 edge case를 계속 점검.

## 2026-06-18 10:18 KST
- 요구사항3 재점검:
  - `alternative/요구사항3` 원문을 다시 확인해 디자인 후퇴, 드래그 불가, UI 돌출, CLI 과도한 세로 확장, 별도 창, 우클릭 메뉴, 선 표시, `.pkt` 호환 오해를 체크리스트로 다시 잡음.
  - GitHub main 이전 구현과 비교해 이전판의 장점인 링크 라이트/라벨, 포트 점, 선택 흐름을 현 Packet Tracer식 별도 창/우클릭 구조에 반영하는 방향 유지.
- 추가 구현:
  - 상단 바에 Packet Tracer식 `File / Edit / Options / View / Tools / Extensions / Help` 메뉴 라벨 추가.
  - Physical 탭이 단순 배경 변경으로만 보이지 않도록 Wiring Closet/RackView 느낌의 물리 배경 레이어 추가.
  - Physical 모드에 장비 수, 라우터/방화벽 수, 스위치/허브 수, 호스트 수, 무선 수 요약 표시.
  - 장비 창 내부에 `min-width: 0`, auto-fit grid, 포트 테이블 내부 스크롤 규칙 추가.
  - 작은 화면에서 장비 설정 행, 모듈 슬롯, 포트 테이블 행이 한 열로 접히도록 반응형 CSS 추가.
  - 하단 Simulation Events 헤더가 좁은 폭에서 버튼/필터 때문에 삐져나오지 않도록 wrap 처리.
- 검증:
  - `npm run build --workspace web` 통과.
- 다음 작업:
  - 케이블 선택/연결 흐름에서 사용자에게 가능한 포트와 상태가 더 명확히 보이도록 개선.
  - 창 위치, 우클릭 메뉴, 링크 라벨 길이, 하단 트레이 edge case 계속 점검.

## 2026-06-18 10:29 KST
- 추가 구현:
  - 케이블 선택 중 포트 점 상태를 Packet Tracer식으로 구분:
    - 사용 가능 후보 포트는 녹색/펄스 표시.
    - 첫 번째 선택 장비의 포트는 pending 색상 표시.
    - 사용 중이거나 호환되지 않는 포트는 흐린 blocked 표시.
  - 캔버스 링크 라벨을 전체 긴 문장 대신 `케이블 타입 + 양끝 포트 + 상태`의 짧은 표기로 변경하고, 전체 endpoint 정보는 SVG title로 유지.
  - PDU 봉투 버튼 클릭 시 노드 드래그가 같이 시작될 수 있는 edge case 차단.
  - 장비 우클릭 메뉴와 링크 우클릭 메뉴가 동시에 남지 않도록 정리.
  - 홈 화면 Import accept에서 `.pkt` 제거. JSON/PTWEB만 선택되게 변경하고, title에 Cisco `.pkt` 미지원 명시.
  - JSON export 아이콘을 PTWEB export와 구분되도록 `FileJson` 아이콘으로 변경.
  - 하단 Cables 목록의 긴 링크 라벨/메시지 overflow를 ellipsis 처리.
  - Desktop 탭을 Packet Tracer에 가까운 앱 선택 구조로 변경:
    - `IP Configuration`: 포트별 IPv4/Mask/Gateway/DNS 입력 및 DHCP 갱신.
    - `Command Prompt`: 기존 ipconfig/ping/nslookup/http 명령 유지.
    - `Web Browser`: HTTP 서비스 테스트용 주소 입력/결과 출력.
- 검증:
  - `npm run build --workspace web` 통과.
  - dev server 응답 유지 확인: `http://localhost:5173/`.
- 다음 작업:
  - Services 탭/Config 탭의 Packet Tracer식 조밀도와 창 내부 overflow 추가 점검.
  - Simulation Events/User Created Packet 흐름이 하단 dock에서 더 명확히 보이도록 개선.

## 2026-06-18 10:59 KST
- 웹/문서 재확인:
  - `https://tutorials.ptnetacad.net/help/default/interfaceOverview.htm` HTTP 200 OK 확인.
  - `https://www.netacad.com/cisco-packet-tracer` HTTP 200 OK 확인.
  - `alternative/조사.txt`의 오래된 403 문구를 오늘 확인 기준으로 정정.
- 추가 구현:
  - Realtime/Simulation 탭의 내용 분리:
    - Realtime은 링크 up/전체 수, 진단, 케이블 상태 중심.
    - Simulation은 Event List, 필터, Prev/Next/Clear, PDU Information 중심.
  - Simulation Event List 하단에 선택/최근 이벤트의 PDU Information과 OSI layer chip 추가.
  - Event List가 비어 있을 때 빈 상태 행 표시.
  - Services 탭을 Packet Tracer식 서비스 목록 + 상세 패널 구조로 재배치:
    - HTTP/DHCP/DNS/TFTP/SYSLOG를 왼쪽 목록에서 선택.
    - DHCP pool/lease, DNS record 편집은 상세 패널 안에서 유지.
  - Config 탭 상단에 Interface/Routes/VLAN/Wireless/Security 빠른 이동 버튼 추가.
  - 장비 창에 Maximize/Restore 버튼 추가. 최대화 시 CLI/터미널 높이도 함께 확장.
  - Escape 키로 선택, 케이블 모드, 컨텍스트 메뉴, 장비 창까지 정리되도록 보정.
  - 케이블 선택 후 두 장비 사이에 가능한 포트쌍이 여러 개면 즉시 연결하지 않고 Connection Assistant를 띄워 정확한 포트를 선택하게 변경.
  - 하단 Connections 버튼에 케이블 타입별 색상 swatch 추가.
  - 장비 배치 모드에서 선택된 모델 HUD 표시.
  - 배치 모드에서는 기존 노드 드래그/열기 동작이 끼어들지 않도록 모드 충돌 차단.
  - 링크 우클릭 메뉴에 상태 원인 표시 추가: power off, shutdown, serial clock rate, wireless mismatch/range, trunk VLAN mismatch 등.
  - Wireless 링크는 직선이 아니라 곡선 dashed path로 표시하고, 라벨/곡선 좌표가 캔버스 상단 밖으로 나가지 않게 clamp 처리.
  - Physical 탭의 모듈 Install/Remove/select를 전원이 켜진 상태에서 비활성화.
  - README에 `.ptweb`은 자체 포맷이며 Cisco Packet Tracer 6.1 proprietary `.pkt` binary export는 미구현이라고 명시.
- 검증:
  - `npm run build --workspace web` 반복 통과.
  - dev server 응답 유지 확인: `http://localhost:5173/` 200 OK.
  - `window.prompt`, `confirm`, `alert`, `.pkt` export 호출, `.pkt` import accept 잔여 코드 없음 확인.
- 제한:
  - Playwright screenshot 검증은 앞서 확인한 `libnspr4.so` 누락 때문에 계속 보류.
- 다음 작업:
  - 실제 장비 배치/연결/설정 흐름을 계속 코드 기준으로 따라가며 Packet Tracer와 다른 잔여 UX를 줄임.
  - 11:30 전후 다음 개발일지 갱신.

## 2026-06-18 11:26 KST
- 요구사항3 추가 반영:
  - 이전 main 디자인에서 좋았던 링크 선택 카드 흐름을 현재 Packet Tracer식 UI에 재도입.
  - 케이블/링크 클릭 시 캔버스 위에 선택 카드 표시:
    - 케이블 타입, 양끝 endpoint, 현재 status, status 원인 표시.
    - 카드에서 바로 `Remove Cable` 가능.
    - 선택된 링크는 선 굵기/라벨/라이트가 강조되도록 CSS 추가.
  - 빈 워크스페이스 우클릭도 Chrome 기본 메뉴 대신 사이트 메뉴가 뜨도록 변경:
    - Select Mode, Zoom 100%, Logical, Physical, Repair Project 제공.
  - 장비/링크/팔레트 조작 시 열린 컨텍스트 메뉴가 남지 않도록 상태 정리.
  - Simulation dock에 Packet Tracer의 User Created Packet Window에 가까운 최근 사용자 패킷 표 추가:
    - ICMP/DHCP/DNS/HTTP 이벤트를 최근 순으로 표시.
    - Protocol, Source, Destination, Status를 별도 표로 표시.
    - 패킷 행 클릭 시 해당 simulation event에 focus.
    - 작은 화면에서 protocol/status 열이 줄어들도록 반응형 CSS 추가.
- 구형 main/기존 저장물 호환:
  - `normalizeProject`에서 legacy 장비 필드 보정 강화:
    - `x/y`, `type`, `catalogId`, `modelName` fallback 유지.
    - legacy `moduleSlots`의 installed module을 현재 `modules` 형태로 변환.
  - legacy 포트 보정 추가:
    - `interfaceConfig.ipAddress/subnetMask/gateway/dns`를 현재 포트 필드로 이관.
    - legacy `gigabit` 포트 타입을 `gigabit-ethernet`으로 변환.
    - 미지원/알 수 없는 포트 타입은 import가 깨지지 않도록 `ethernet`으로 정규화.
    - `administratively-down` 상태를 현재 `adminUp: false`로 반영.
  - legacy 링크 보정 추가:
    - old `a/b` endpoint를 `endpointA/endpointB`로 변환하는 로직을 import 경로뿐 아니라 localStorage normalize 경로에도 적용.
    - old `dceEndpoint: a/b`를 현재 `A/B`로 변환.
    - old/미지원 cable type과 link status를 현재 지원 값으로 정규화.
  - legacy config/runtime 보정 추가:
    - old static route `destination`, DNS `host/address`, DHCP `defaultRouter`, ACL `firewallRules`, wireless `security/wepKey`, runtime arp/mac/dhcpLeases를 현재 구조로 변환.
- 검증:
  - `npm run build --workspace web` 통과.
  - dev server 응답 유지 확인: `http://localhost:5173/` 200 OK.
  - `window.prompt`, `confirm`, `alert`, `.pkt` export 호출, `.pkt` import accept 잔여 코드 없음 확인.
- 계속 진행:
  - 하단 dock 조밀도와 장비 창 내부의 긴 테이블/폼 edge case를 계속 다듬을 예정.

## 2026-06-18 12:00 KST
- 요구사항3 기준 재대조:
  - NetAcad Packet Tracer interface/simulation 도움말 기준의 상단 메뉴, Logical/Physical, Realtime/Simulation, Network Component Box, Event List/PDU 정보, 별도 장비 창 구조를 다시 맞춰 봄.
  - GitHub `deploy103/Network-Editor-Web` main 확인: 현재 공개 main commit은 `f66fea0 Initial public release` 1개뿐이고, `/tmp/network-editor-web-main` 이전 코드에서 링크 라이트/선 라벨/포트 점/하단 팔레트 흐름을 다시 참고함.
- 추가 구현:
  - 상단 `File / Edit / Options / View / Tools / Extensions / Help` 라벨을 실제 dropdown 메뉴로 동작하게 유지하고, 장비/링크/워크스페이스/팔레트/드래그/PDU 클릭 시 열린 상단 메뉴와 컨텍스트 메뉴가 남지 않도록 `closeFloatingMenus` 흐름 적용.
  - 장비 우클릭 시 이전에 선택된 링크 카드가 남는 문제를 정리하고, 링크 클릭/우클릭 시 장비 메뉴/워크스페이스 메뉴/상단 메뉴가 같이 닫히도록 보정.
  - 링크 선택 카드 강화:
    - 케이블 타입/status뿐 아니라 A/B endpoint별 장비명, 포트명, access/trunk/routed 모드, 전원/포트 shutdown 상태 표시.
    - 하단 Cables 목록에 cable swatch를 붙이고 status reason을 계속 표시.
  - Simulation/Realtime dock 강화:
    - Simulation Events 상단에 total/forwarded/delivered/dropped/shown 요약 strip 추가.
    - Realtime Status 상단에 devices/cables/up/down/blocked 요약 strip 추가.
  - Connection Assistant 강화:
    - 단순 select 외에 포트별 ready/used/incompatible 클릭 목록 추가.
    - 포트별 short name, kind, valid pair/wrong cable/already used/not valid reason 표시.
    - 연결 실패/포트 선택 창이 일반 장비 창 뒤로 깔리지 않도록 warning window z-index 상향.
- 검증:
  - `npm run build --workspace web` 반복 통과.
  - dev server 응답 유지 확인: `http://localhost:5173/` 200 OK.
  - `window.prompt`, `confirm`, `alert`, `.pkt` export 호출, `.pkt` import accept 잔여 코드 없음 확인.
- 제한:
  - Playwright screenshot 검증은 시스템 `libnspr4.so` 누락으로 계속 보류.
- 계속 진행:
  - 12:30 전후 다음 개발일지 갱신 예정.
  - 다음 패스는 장비 창 내부 Config/Physical/Services의 긴 폼 overflow와 Packet Tracer식 조밀도 추가 점검.

## 2026-06-18 12:30 KST
- 런타임 검증/수정:
  - dev server가 `/mnt/c` 경로 변경을 HMR로 제대로 반영하지 않아, 소스에는 `packet-menu-labels`가 있는데 실제 화면에는 상단 메뉴가 안 보이던 원인을 확인.
  - Vite 서버를 재시작한 뒤 `/src/components/Editor.tsx` 응답에서 `packet-menu-labels`, `PacketMenuDropdown`이 실제로 내려오는지 확인.
  - 이후 CSS 변경도 같은 watch 문제로 stale 상태가 반복되어, 검증 시 dev server 재기동 후 `curl`로 런타임 소스/CSS 반영 여부를 확인하는 방식으로 전환.
- Windows Chrome CDP 검증:
  - Linux Playwright는 시스템 라이브러리 누락으로 계속 실패하므로, Windows Chrome headless + CDP 자동화 스크립트로 테스트 사용자/샘플 프로젝트를 만들고 에디터 캡처.
  - 캡처/검사 결과:
    - 상단 `File/Edit/Options/View/Tools/Extensions/Help` 메뉴 DOM 표시 확인.
    - 별도 Device Window 표시 확인.
    - 샘플 프로젝트 장비 4개, 링크 3개, 하단 Packet Tracer식 palette, Event Panel 표시 확인.
  - 최신 캡처 파일: `C:\Temp\network-editor-editor-shot-1218.png`.
- 추가 구현:
  - Config 탭을 Packet Tracer 장비 창에 더 가깝게 좌측 카테고리 레일 + 우측 설정폼 구조로 변경.
  - `input[type="checkbox"]`가 기존 `width: 100%` 규칙을 상속해 Admin checkbox가 가운데로 밀리던 문제 수정.
  - Config 폼 label/input 정렬을 고정하고, 입력값이 label 굵기를 상속해 과하게 굵어지던 문제 수정.
  - `DHCP Renew` 버튼이 전폭으로 늘어나던 문제를 Packet Tracer식 작은 명령 버튼 형태로 보정.
- 검증:
  - `npm run build --workspace web` 통과.
  - dev server 재기동 후 `curl -I http://localhost:5173/` 200 OK 확인.
  - Windows Chrome CDP 캡처로 Config 탭 정렬, checkbox 위치, DHCP 버튼 폭 보정 확인.
- 계속 진행:
  - 다음 패스는 CLI/Desktop/Services 탭의 창 내부 overflow, 우클릭/드래그 실제 동작, 하단 dock 밀림을 계속 점검.

## 2026-06-18 13:00 KST
- 웹/레퍼런스 재확인:
  - Packet Tracer 구조를 다시 기준으로 잡음: 상단 메뉴/툴바, Logical/Physical 작업공간, 하단 Device-Type/Device-Specific/Connections 박스, Realtime/Simulation/Event List, 장비별 Physical/Config/CLI/Desktop/Services 별도 창.
  - GitHub 원격 `deploy103/Network-Editor-Web`의 기본 브랜치가 `main`이고 공개 커밋은 `f66fea0 Initial public release` 1개인 점을 재확인. `/tmp/network-editor-web-main` 이전 코드의 링크 라이트/라벨/포트 점/하단 팔레트 흐름을 계속 참고.
- 검증:
  - Vite stale cache 문제 때문에 dev server를 재시작하고 `curl -I http://localhost:5173/` 200 OK 확인.
  - Windows Chrome CDP로 Desktop Command Prompt 캡처 재검증:
    - Desktop 탭, Command Prompt 앱, `PC0>` 프롬프트, 입력줄 표시 확인.
    - 최신 캡처: `C:\Temp\network-editor-desktop-1240.png`.
  - Windows Chrome CDP 상호작용 검증:
    - 노드 드래그 이동 성공(`moved: true`).
    - 장비 우클릭 메뉴가 `Open`, `CLI`, `Rename`, `Power Off`, `Delete`로 표시됨.
    - CLI가 별도 Device Window의 `CLI` 탭으로 열림.
    - 상단 `File` 드롭다운이 `Save`, `Export JSON`, `Export PTWEB`, `Back to Projects`로 동작함.
- 현재 문제/다음 수정:
  - Desktop Command Prompt 입력줄 잘림은 해결됨.
  - 장비 창 하단에 빈 여백이 크게 남는 상태가 보여, Packet Tracer 창처럼 내용 높이에 맞게 더 조밀하게 줄이고 내부 스크롤을 정리할 예정.

## 2026-06-18 13:30 KST
- 추가 구현/수정:
  - 장비 창 body를 `헤더 / 탭 / 탭 내용` 3행 구조로 변경해 헤더와 탭은 고정하고 실제 탭 내용만 스크롤되도록 수정.
  - Desktop Command Prompt/CLI terminal 높이를 장비 창 내부 남은 영역에 맞게 채우도록 바꿔 입력줄이 잘리거나 하단에 큰 공백이 남지 않게 조정.
  - Services 탭 DHCP/DNS 입력을 placeholder만 있는 큰 타일형 입력에서 Packet Tracer식 라벨 있는 조밀한 폼으로 변경.
  - Physical 탭을 `Modules` 목록 + `Chassis/Slots/Ports` 구조로 재구성:
    - 왼쪽 모듈 목록에서 호환 모듈을 선택 가능.
    - 오른쪽에서 전원, 포트 전면부, module slot install/remove, port table을 확인.
  - 케이블 연결 흐름 변경:
    - 두 번째 장비를 클릭하면 후보가 하나뿐이어도 바로 연결하지 않고 Connection Assistant를 띄워 포트를 확인/선택하게 변경.
    - Connection Assistant 포트 목록 높이를 줄여 하단 `Connect selected ports`/`Cancel` 버튼이 화면 안에 보이도록 조정.
  - Packet Tracer의 Common Tools Bar에 가까운 워크스페이스 왼쪽 아이콘 툴바 추가:
    - Select, Inspect selected device, Delete selected, Add Simple PDU.
  - Simulation Event List의 강제 `min-width`를 제거하고 6개 열이 하단 dock 안에 맞도록 반응형 grid column으로 수정.
- 검증:
  - `npm run build --workspace web` 반복 통과.
  - Windows Chrome CDP:
    - Desktop 탭 캡처: `C:\Temp\network-editor-desktop-1240.png`, 입력 프롬프트 표시 확인.
    - Router Config/Server Services/PC Physical 탭 캡처: `C:\Temp\network-editor-router-config-1305.png`, `C:\Temp\network-editor-server-services-1305.png`, `C:\Temp\network-editor-pc-physical-1305.png`.
    - Connection Assistant 캡처: `C:\Temp\network-editor-connection-1315.png`, 사용/준비/호환 불가 포트 상태와 버튼 가시성 확인.
    - Mobile viewport 캡처: `C:\Temp\network-editor-mobile-1320.png`, 의도된 캔버스 스크롤 외 장비 창 주요 UI 확인.
    - Simulation dock 캡처: `C:\Temp\network-editor-dock-1328.png`, Event Panel `scrollWidth === clientWidth`로 가로 스크롤 제거 확인.
    - 드래그/우클릭/CLI/File menu 회귀 검증: `moved: true`, 장비 우클릭 메뉴/별도 CLI 창/File dropdown 확인.
  - GitHub main 확인:
    - `deploy103/Network-Editor-Web` 기본 브랜치 `main`.
    - 현재 공개 커밋은 `f66fea0 Initial public release`.
- 계속 진행:
  - 다음 패스는 Simulation/User Created Packets 실제 이벤트 발생 흐름, 하단 dock의 데이터 밀도, 링크 선택 카드/포트 선택 edge case를 더 다듬을 예정.

## 2026-06-18 14:00 KST
- 진행 조건 변경 반영:
  - 사용자가 종료 목표를 `15:50 KST`로 연장했으므로 그 시각까지 개발/검증을 계속 진행.
- 추가 구현/수정:
  - Add Simple PDU를 Common Tools Bar의 실제 도구 모드로 정리:
    - PDU 선택 후 source 장비 선택 시 장비 창이 열리지 않음.
    - destination 후보 장비에 envelope target이 표시됨.
    - 전송 후 자동으로 Simulation 탭으로 전환하고 최신 event/PDU Information에 focus.
  - Simulation 버튼 라벨을 Packet Tracer 흐름에 맞춰 `Back`, `Capture/Forward`, `Auto Capture/Play`, `Clear`로 정리.
  - 하단 Simulation dock을 세로 누적 구조에서 좌우 split 구조로 변경:
    - 왼쪽: Simulation Event List.
    - 오른쪽: User Created Packets + PDU Information.
    - User Created Packets 좁은 패널에서 source/destination 헤더가 `SRC / DST`로 읽히도록 보정.
  - 하단 tray 비율을 palette와 Simulation dock이 비슷한 폭을 갖도록 조정해 Event List/User Created Packets가 튀어나오지 않게 수정.
  - Physical 탭의 모듈 설치/제거 UI 실제 흐름을 점검:
    - 전원 on 상태에서는 Install/Remove 비활성.
    - 전원 off 후 HWIC-2T 설치 시 Serial0/0/0, Serial0/0/1 포트가 추가됨.
    - Remove 후 원래 포트 목록으로 복귀.
- 검증:
  - `npm run build --workspace web` 통과.
  - dev server 재기동 후 `http://localhost:5173/` 200 OK 확인.
  - PDU CDP 검증:
    - source 선택 상태, destination target 3개, 장비 창 미오픈 확인.
    - 전송 후 Simulation 활성화, events 4개, User Created Packets 2개, PDU marker/PDU Information 표시 확인.
    - 캡처: `C:\Temp\network-editor-pdu-1340.png`, 결과: `C:\Temp\network-editor-pdu-1340.json`.
  - Simulation dock CDP 검증:
    - Event Panel `scrollWidth === clientWidth`로 가로 overflow 없음 확인.
    - 캡처: `C:\Temp\network-editor-dock-1328.png`.
  - Physical module CDP 검증:
    - 전원 on 설치 비활성, 전원 off 설치 활성, HWIC-2T 설치/제거 및 포트 수 변화 확인.
    - 캡처: `C:\Temp\network-editor-physical-module-1400.png`, 결과: `C:\Temp\network-editor-physical-module-1400.json`.
  - 연결 성공 CDP 검증:
    - Router1 배치 후 Connection Assistant 표시, link count 3 -> 4, selected link card 표시 확인.
    - 결과: `C:\Temp\network-editor-connection-success-1350.json`.
  - CLI 명령 CDP 검증:
    - 별도 CLI 창에서 `show ip interface brief`, `show cdp neighbors` 출력 확인.
    - 결과: `C:\Temp\network-editor-cli-command-1345.json`.
  - 드래그/우클릭/CLI/File menu 축소 회귀 검증:
    - DOM PointerEvent 기반으로 `moved: true`.
    - 장비 우클릭 메뉴 `Open`, `CLI`, `Rename`, `Power Off`, `Delete` 확인.
    - CLI 별도 창, File dropdown `Save`, `Export JSON`, `Export PTWEB`, `Back to Projects` 확인.
    - 캡처: `C:\Temp\network-editor-interactions-lite-1400.png`, 결과: `C:\Temp\network-editor-interactions-lite-1400.json`.
- 참고:
  - 기존 `network-editor-cdp-interactions.mjs`의 실제 CDP mouse dispatch 버전은 이번 실행에서 PowerShell 프로세스가 멈춰 종료했고, 동일 항목은 축소 회귀 스크립트로 재검증 완료.
- 다음 작업:
  - 링크 선택 카드와 Connection Assistant의 남은 edge case, 모바일/좁은 폭 하단 dock, `.ptweb`/`.pkt` 문구 잔여 여부를 계속 점검.

## 2026-06-18 14:30 KST
- 추가 구현/수정:
  - 링크 선택 카드가 첫 링크 위에서 PC 노드와 겹치던 문제를 완화:
    - 카드 폭/높이/padding을 줄임.
    - 일반 케이블 카드 기준점을 위로 올려 장비 노드 위를 덮지 않게 조정.
    - 최신 링크 카드 캡처에서 card bottom 299px로 PC 노드 위에 위치 확인.
  - 링크 우클릭 메뉴 제목을 내부 값 `copper-straight` 대신 `Cu Straight`처럼 사람이 읽는 케이블명으로 변경.
  - Connection Assistant 포트 목록 개선:
    - 사용 중 포트를 `already used`가 아니라 `used by PC0 FastEthernet0`, `used by Switch0 FastEthernet0/2`처럼 실제 연결 상대와 함께 표시.
    - 실패 케이스에서도 어떤 포트를 비워야 하는지 더 명확하게 보이도록 수정.
  - User Created Packets를 Packet Tracer식 “사용자가 만든 PDU 단위”에 더 가깝게 수정:
    - `SimulationEvent`에 optional `sourceDeviceId`, `targetDeviceId`, `packetId` 추가.
    - TypeScript fallback, WASM wrapper, desktop DNS/HTTP/DHCP release event, legacy normalize 경로에서 packet metadata 보강.
    - 하단 User Created Packets가 같은 Simple PDU의 여러 ICMP hop을 2줄로 보여주지 않고 최신 상태 1줄로 묶도록 변경.
    - 좁은 패널 헤더는 `SRC/DST`로 축약.
- 검증:
  - `npm run build --workspace web` 통과.
  - dev server 재기동 및 `http://localhost:5173/` 200 OK 확인.
  - PDU CDP 검증:
    - source 선택, destination target 표시, 장비 창 미오픈, Simulation 전환 유지.
    - 전송 후 events 4개, User Created Packets 1개, PDU marker/PDU Information 표시 확인.
    - 캡처/결과: `C:\Temp\network-editor-pdu-1340.png`, `C:\Temp\network-editor-pdu-1340.json`.
  - 링크 카드 CDP 검증:
    - selected link card 표시, selected link 강조, 카드 위치가 장비 노드와 겹치지 않음.
    - 캡처/결과: `C:\Temp\network-editor-link-1345.png`, `C:\Temp\network-editor-link-1345.json`.
  - Workspace/Link 우클릭 CDP 검증:
    - 빈 workspace 메뉴: `Select Mode`, `Zoom 100%`, `Logical`, `Physical`, `Repair Project`.
    - 링크 메뉴: `Cu Straight`, link status detail, `Remove Cable`.
    - 캡처/결과: `C:\Temp\network-editor-context-menus-1415.png`, `C:\Temp\network-editor-context-menus-1415.json`.
  - Connection Assistant CDP 검증:
    - 성공 케이스: Router1 배치, Connection Assistant 표시, link count 3 -> 4, selected link card 표시.
    - 실패 케이스: Router0 포트 부족 메시지와 `used by ...` 포트 상태 표시, primary button disabled 유지.
    - 결과: `C:\Temp\network-editor-connection-success-1350.json`, `C:\Temp\network-editor-connection-1315.json`.
  - 모바일/좁은 폭 검증:
    - 장비 창 주요 UI가 viewport 안에 들어오며, 의도된 canvas/SVG workspace 외 비정상 overflow 없음.
    - 캡처/결과: `C:\Temp\network-editor-mobile-1320.png`, `C:\Temp\network-editor-mobile-1320.json`.
  - 장비 창 탭별 overflow 검증:
    - Router Config, Server Services, PC Physical 모두 `overflowing: []`.
    - 결과: `C:\Temp\network-editor-tabs-check-1305.json`.
  - Desktop/CLI 회귀:
    - Desktop Command Prompt `PC0>` 표시 확인.
    - CLI `show ip interface brief`, `show cdp neighbors` 출력 확인.
  - `.ptweb`/`.pkt` 및 browser dialog 패턴 재점검:
    - 코드 경로에 `.pkt` export/import 허용 패턴 없음.
    - `window.prompt/confirm/alert` 호출 없음.
  - GitHub 확인:
    - 원격 `deploy103/Network-Editor-Web` 기본 브랜치는 `main`.
    - 공개 커밋은 `f66fea0 Initial public release`.
- 계속 진행:
  - 15:00까지 전체 회귀를 다시 묶어서 확인하고, 남는 시간에는 Packet Tracer식 조밀도/창 위치/하단 dock 가독성을 추가 보정.

## 2026-06-18 15:00 KST
- 전체 회귀 재검증:
  - `npm run build --workspace web` 통과.
  - Vite dev server 재시작 후 `http://localhost:5173/` 200 OK 확인.
  - 실제 Vite 변환 소스에서 상단 Packet Tracer 메뉴/드롭다운 코드가 내려오는지 `curl /src/components/Editor.tsx`로 확인.
- PDU/Simulation 검증:
  - Add Simple PDU source 선택 시 장비 창이 열리지 않음.
  - destination 선택 후 Simulation 탭으로 자동 전환.
  - Event List 4개 hop, User Created Packets 1개 묶음, PDU marker, PDU Information 표시 확인.
  - Capture/Forward가 첫 ARP event에서 SWITCH event로 진행하는 흐름 확인.
- 모바일/창 배치 보정:
  - 작은 viewport에서 floating device window가 화면 왼쪽 밖으로 밀리지 않도록 mobile media query에서 위치를 clamp.
  - 모바일 CDP 캡처 기준 장비 창 주요 UI가 viewport 안에 들어오는 것 확인.
- 탭/overflow 검증:
  - Router Config, Server Services, PC Physical 탭을 `tabs-lite` CDP 스크립트로 재검증.
  - 기존 full tabs 스크립트는 한 번 멈춰서 경량 스크립트로 대체했고, 세 탭 모두 `overflow: []` 확인.
- 안전 점검:
  - `window.prompt/confirm/alert` 호출 없음.
  - `.pkt` export/import 허용 패턴 없음.
  - 자체 포맷은 `.ptweb`, Cisco Packet Tracer 6.1 proprietary `.pkt` binary export는 미구현이라는 문구 유지.

## 2026-06-18 15:30 KST
- Packet Tracer 레퍼런스 재대조:
  - NetAcad Packet Tracer help 기준 상단 메뉴/툴바, Logical/Physical, Network Component Box, Realtime/Simulation, Event List/User Created Packet, 장비별 floating window 구조를 다시 확인.
  - GitHub 원격 `deploy103/Network-Editor-Web`의 `main`은 공개 커밋 `f66fea0 Initial public release` 1개뿐인 것을 재확인.
  - `/tmp/network-editor-web-main` 이전 코드에서 하단 팔레트/링크 라이트/포트 점/선 라벨 흐름을 계속 참고.
- 추가 구현:
  - 공식 Packet Tracer 상단 메뉴 구성에 맞춰 `Window` 메뉴를 추가.
  - `Window` 메뉴에 Enter/Exit Full Screen, Open Selected Device, Open Selected CLI, Activate/Close Device Window, Simulation Panel 항목 추가.
  - 선택된 장비가 있으면 Window 메뉴에서 바로 장비 창/CLI 창을 열 수 있게 연결.
- 검증:
  - production build 통과.
  - Vite dev server 재시작 후 실제 `/src/components/Editor.tsx` 응답에 `Window`, `Enter Full Screen` 반영 확인.
  - Windows Chrome CDP `network-editor-cdp-window-menu-check.mjs` 추가:
    - 메뉴 라벨 `File/Edit/Options/View/Tools/Extensions/Window/Help` 확인.
    - Window dropdown title과 Full Screen, Open Selected Device, Open Selected CLI, Close Device Window, Simulation Panel 항목 확인.
  - Workspace/Link 우클릭 메뉴 재검증:
    - 빈 workspace 메뉴: Select Mode, Zoom 100%, Logical, Physical, Repair Project.
    - 링크 메뉴: `Cu Straight`, endpoint, status, Remove Cable 표시.

## 2026-06-18 15:50 KST
- 최종 구현 상태:
  - Packet Tracer식 상단 메뉴에 `Window`까지 포함해 `File/Edit/Options/View/Tools/Extensions/Window/Help` 구조로 보정.
  - Device Window/CLI Window, 우클릭 사이트 메뉴, 링크 카드, Connection Assistant, Common Tools Bar, Add Simple PDU, Simulation dock, Physical module 흐름 유지.
  - `.pkt` 호환 오해를 줄이기 위해 export/import는 JSON/PTWEB만 유지하고 Cisco Packet Tracer 6.1 proprietary `.pkt` binary export 미구현 안내 유지.
- 최종 검증:
  - `npm run build --workspace web` 통과.
  - `window.prompt/confirm/alert` 패턴 없음.
  - `.pkt` export/import 허용 패턴 없음.
  - Vite dev server 재시작 후 `http://localhost:5173/` 200 OK 확인.
  - Windows Chrome CDP:
    - Window 메뉴 dropdown 정상 확인: Full Screen, Open Selected Device, Open Selected CLI, Close Device Window, Simulation Panel.
    - PDU 생성 정상 확인: Simulation 전환, events 4개, User Created Packets 1개, PDU Information 표시.
    - Capture/Forward 정상 확인: ARP event에서 SWITCH event로 진행.
    - workspace/link 우클릭 메뉴 정상 확인.
- GitHub 푸시 준비:
  - 현재 작업 폴더에는 `.git`이 없어 원격 `deploy103/Network-Editor-Web`을 임시 클론한 뒤 현재 작업물을 동기화하는 방식으로 커밋/푸시 진행.
  - 원격 기본 브랜치 `main`, viewer permission `ADMIN` 확인.

## 2026-06-19 09:20 KST
- 마감형 기능 보강:
  - 프로젝트 홈에 검색, 정렬, 깊은 복제, 삭제 확인, 작업 알림, 가져오기 미리보기 추가.
  - 에디터 상태바에 저장 대기/저장 중/저장 완료 시각/저장 오류 상태 표시 추가.
  - Tools 메뉴에 진단 리포트 내보내기, 장비 자동 정렬, 선택 장비 전체 Ping, 런타임 테이블 초기화 추가.
  - 데스크톱 명령 프롬프트에 빠른 명령 버튼과 히스토리 이동 추가.
  - Desktop `tftp`, `syslog` 명령을 추가하고 Simulation Event/User Created Packet 필터에 TFTP/SYSLOG 반영.
  - Services 탭에 TFTP 상태, SYSLOG 로그 표시/비우기, DHCP excluded range 관리 UI 추가.
  - DHCP helper-address UI와 fallback simulation의 DHCP relay 경로 추가.
  - 라우팅 HTTP 샘플 랩을 DHCP relay, DNS, HTTP, TFTP, SYSLOG까지 바로 검증 가능한 구성으로 확장.
- 저장/가져오기 안정성:
  - pagehide/visibilitychange/logout 시 pending save flush 보강.
  - 인증 입력 trim, PTWEB/JSON import BOM/앞공백 처리, 가져오기 5MB/바이너리 제한 미리보기 단계 반영.
  - 프로젝트 복제 시 프로젝트/장비/포트/링크 ID를 새로 발급하고 런타임/이벤트는 초기화.
- 검증:
  - `npm run build --workspace web` 반복 통과.
  - `npm run smoke:features` 추가 및 통과:
    - 샘플 랩 DHCP/DNS/HTTP/TFTP/SYSLOG 서비스 활성 확인.
    - DHCP helper-address 및 excluded range 확인.
    - PC DHCP relay renew가 `192.168.10.100`을 할당하고 helper-address 이벤트를 기록하는지 확인.
  - `npm run smoke:all` 추가 및 통과:
    - feature smoke, TypeScript CLI smoke, Python CLI smoke, Python HTTP smoke 모두 통과.
  - Vite dev server `http://localhost:5173/` 200 OK 확인.

## 2026-06-19 09:40 KST
- 마감형 안정화 추가:
  - DHCP relay가 서버의 첫 활성 풀이 아니라 relay/client subnet에 맞는 DHCP 풀을 우선 선택하도록 보강.
  - DHCP helper-address 진단에 잘못된 IP와 활성 DHCP 서버 부재 케이스 추가.
  - DHCP excluded range 진단에 잘못된 IP, 역순 범위, 활성 풀 밖 범위 감지 추가.
  - 진단 패널에 오류/경고/정보 요약과 숨겨진 추가 이슈 수 표시 추가.
  - 진단 리포트에 서비스 상태, DHCP pool/excluded range, DNS record, SYSLOG log 요약 추가.
  - CLI `show services` 추가 및 TFTP/SYSLOG service 자동완성/저장 복구 smoke 보강.
  - 저장 요청 race에서 오래된 저장 성공/실패가 최신 저장 상태를 덮어쓰지 않도록 방어.
  - 상태바 저장 배지/엔진 라벨 간격 CSS 정리.
  - DHCP 할당이 이미 인터페이스에 사용 중인 IP와 선택된 풀 밖 기존 lease를 재사용하지 않도록 보강.
  - switchport access/trunk 전환 시 helper/NAT/ACL 같은 L3 전용 포트 필드를 정리하도록 UI/CLI 동작 보정.
  - JSON/PTWEB 가져오기 미리보기 로직을 순수 유틸로 분리하고 BOM/앞공백/PTWEB envelope/구조 오류 smoke 추가.
  - normalizeProject standalone smoke를 추가하고 ACL action 타입 추론을 명시화.
  - subnet mask 연속성 검증을 추가하고 진단/CLI/DHCP 요청/ipInSubnet 계산에 같은 기준 적용.
  - 프로젝트 홈 검색 색인과 작업공간 통계에 HTTP/DHCP/DNS/TFTP/SYSLOG, DHCP pool, DNS record 정보를 반영.
- 검증:
  - `npm run check:web` 통과.
  - 추가 변경 후 `npm run build --workspace web` 반복 통과.
  - `npm run smoke:features`에 DHCP helper/excluded range 진단, subnet mask 진단, multi-pool relay 선택, 기존 lease 무시, 사용 중 IP 회피, normalize/import preview 회귀 추가.
  - `npm run smoke:cli`에 `do show services`, TFTP/SYSLOG service 저장 복구, L2 mode 전환 시 L3 필드 정리, 비연속 mask 입력 거부 회귀 추가.

## 2026-06-19 10:15 KST
- 마감 전 품질 보강:
  - 시뮬레이션 이벤트 행에 Enter/Space 키보드 활성화, 접근성 라벨, 선택 상태 표시, 포커스 스타일을 추가.
  - 미니맵 키보드 대상에 접근성 라벨과 포커스 표시를 추가.
  - Config 탭 정적 라우트 추가가 CLI와 같은 연속 subnet mask 기준을 사용하도록 보정.
  - Services 탭 DHCP 풀 추가가 연속 subnet mask, subnet 내부 시작 IP, subnet 내부 기본 게이트웨이를 검사하도록 보정.
  - CLI DHCP pool 모드에서 네트워크가 설정된 뒤 `default-router`와 `start-ip`가 DHCP 네트워크 밖이면 즉시 거절하고, 새 풀의 입력 순서 유연성은 유지하도록 보정.
  - DHCP 요청 루프가 풀 subnet 밖의 시작 IP를 거부하고 후보 주소도 subnet 안에서만 할당하도록 보강.
  - DHCP 시작 IP/gateway가 풀 subnet 밖인 경우 진단 severity를 실제 실패 조건에 맞춰 error로 조정.
  - 프로젝트 홈/Config/Services 알림의 오류 판정과 `alert`/`status` 역할을 보강.
- 검증:
  - `npm run smoke:features`에 DHCP 시작 IP/gateway가 풀 subnet 밖인 경우 error 진단 및 할당 거부 회귀 테스트 추가 및 통과.
  - `npm run smoke:cli`에 DHCP pool default-router/start-ip subnet 범위와 새 풀 입력 순서 회귀 테스트 추가 및 통과.
  - `npm run build --workspace web` 통과.

## 2026-06-23 11:00 KST
- Packet Tracer식 기능 추가:
  - Common Tools Bar에 Add Complex PDU 도구를 추가.
  - Simple PDU와 별도 상태(`complexPduMode`)로 관리해서 장비 선택/드래그/삭제/팔레트 선택 흐름과 충돌하지 않게 정리.
  - Complex PDU HUD에 프로토콜 선택을 추가: ICMP Echo, DNS Query, HTTP GET, TFTP Read, SYSLOG.
  - Complex PDU source 선택 후 destination 장비에 별도 target badge를 표시.
  - ICMP는 기존 `simulatePing` 경로를 재사용하고, DNS/HTTP/TFTP/SYSLOG는 경로 도달성 확인 후 대상 서비스 상태에 따라 Layer 7 Simulation Event를 delivered/dropped로 기록.
  - SYSLOG delivered 시 대상 서버 runtime log에도 Complex PDU 테스트 로그를 남김.
  - Event List/User Created Packets 기존 필터가 DNS/HTTP/TFTP/SYSLOG를 이미 지원하므로 새 이벤트가 하단 Simulation dock에 바로 누적됨.
- UI/CSS 보강:
  - Complex PDU 모드에서 workspace cursor를 crosshair로 표시.
  - Complex PDU HUD의 select 크기와 target badge 색상을 보정.
  - 빈 보드 안내, 선택 장비 HUD, 드래그 이동은 Complex PDU 도구 선택 중에는 끼어들지 않도록 조건 보강.
  - 장비 노드에 Packet Tracer식 포트 LED 스트립을 추가.
  - 포트 LED는 free/up/down/blocked/shutdown 상태를 회색/녹색/빨강/amber로 표시하고, 스위치처럼 포트가 많은 장비는 첫 14개로 제한해 노드 크기 흔들림을 줄임.
  - Desktop Command Prompt의 기존 `tracert`/`traceroute` 처리 로직을 빠른 명령 팔레트와 placeholder에 노출해 PC에서 경로 추적을 바로 실행할 수 있게 보정.
  - Common Tools Bar와 상단 Tools 메뉴가 같은 `startSimplePduTool`/`startComplexPduTool` 헬퍼를 사용하도록 정리.
  - 상단 Tools 메뉴에 `Simple PDU 추가`, `Complex PDU 추가`를 추가해 Packet Tracer식 메뉴/툴바 접근 경로를 맞춤.
  - Complex PDU HUD에 반복 횟수 입력을 추가하고 1~10회 범위로 제한.
  - Complex PDU 전송 함수가 반복 횟수만큼 순차 실행되며, 각 반복 결과를 Simulation Event에 누적하고 성공/드롭 수를 상태 메시지로 요약하도록 보강.
  - 반복 SYSLOG PDU는 서버 runtime log에도 반복 번호가 포함된 로그를 남김.
  - 반복 Complex PDU의 앱 계층 이벤트가 User Created Packets에 여러 줄로 흩어지지 않도록 같은 packetId로 묶어 기록하게 보정.
  - `appendDesktopEvent`는 선택적 packetId를 받도록 확장하고, 기존 호출은 기본값을 유지해 기존 Desktop/DNS/HTTP/TFTP/SYSLOG 흐름과 호환되게 처리.
  - User Created Packets의 프로토콜 칸에 같은 packetId로 묶인 이벤트 수를 `xN` 형태로 표시해 다중 hop/반복 PDU를 한 줄에서 파악할 수 있게 보정.
  - PDU Information 패널에 출발지, 목적지, 현재 장비, packetId 뒷자리 메타 정보를 추가.
  - OSI layer chip 목록에 전용 클래스를 붙여 새 PDU 메타 grid와 CSS가 충돌하지 않게 정리.
  - 장비 노드 포트 LED 스트립에서 14개를 초과하는 포트는 `+N` 마커로 숨겨진 수를 표시.
- 검증:
  - `npm run build --workspace web` 9회 통과.
  - `npm run smoke:features` 통과.
  - `npm run smoke:cli` 통과.
  - 최종 묶음 기준 `npm run smoke:all` 통과:
    - feature smoke, TypeScript CLI smoke, Python CLI smoke, Python HTTP smoke 모두 통과.
  - Vite/Rolldown 번들 크기 경고는 기존 수준의 경고로 기능 빌드 실패는 아님.
- TODO:
  - Complex PDU에 시작 시간/TTL/포트 번호/반복 간격 같은 Packet Tracer 고급 옵션은 아직 없음.
  - 실제 브라우저 조작 캡처로 Complex PDU source/destination 클릭 흐름을 추가 검증해야 함.
  - 현재 작업 폴더는 `.git`이 없어 push 단계에서 원격 동기화 방식 확인 필요.

## 2026-06-23 11:19 KST
- GitHub push:
  - 현재 작업 폴더에 `.git`이 없어 `/tmp/network-editor-web-push-6B0BqQ/repo`에 `deploy103/Network-Editor-Web`을 임시 클론.
  - `.gitignore` 기준으로 `node_modules`, `dist`, `web/dist`, Rust/Go build output, `.env`, Python cache를 제외하고 작업 폴더를 동기화.
  - WSL/Windows 권한 차이로 생긴 mode-only 변경은 `core.filemode=false`로 제거하고 실제 내용 변경만 커밋.
  - 커밋 `6115f0b Add Packet Tracer-style simulation tools`를 `origin/main`에 push 완료.
  - 원격 확인: `refs/heads/main`이 `6115f0b4519bf0f853e0437bda472cde91555fe4`를 가리킴.
  - push 결과를 포함한 개발일지 갱신분은 `1b1ac4c Update development log after push`로 추가 push 완료.

## 2026-06-23 11:24 KST
- 추가 개발:
  - Tools 메뉴에 `시뮬레이션 이벤트 CSV 내보내기` 추가.
  - Event List의 time/type/status/source/target/lastDevice/atDevice/packetId/info/osiLayers를 CSV로 다운로드.
  - 이벤트 설명에 쉼표, 따옴표, 줄바꿈이 있어도 깨지지 않도록 CSV escaping 헬퍼 추가.
  - Simulation Event List 헤더에도 `CSV` 버튼을 추가해 Tools 메뉴를 거치지 않고 캡처 패널에서 바로 이벤트를 내보낼 수 있게 보정.
  - 장비 우클릭 메뉴에 `Complex PDU 보내기`를 추가.
  - 우클릭으로 시작한 Complex PDU는 해당 장비를 source로 고정하고, HUD에서 프로토콜/횟수 확인 후 destination 장비를 선택하는 흐름으로 동작.
  - PDU Information 패널에 같은 packetId로 묶인 최근 이벤트 흐름을 최대 8단계 timeline으로 표시.
  - Timeline은 `lastDevice -> atDevice`, protocol/status를 함께 보여 경로와 dropped 지점을 빠르게 확인할 수 있게 구성.
  - Capture/Forward로 선택된 이벤트가 timeline 안에서도 강조되도록 selected 스타일 추가.
- 검증:
  - `npm run build --workspace web` 5회 통과.
  - `npm run smoke:features` 통과.
- TODO:
  - CSV export는 현재 클라이언트 다운로드만 지원하며 서버 저장/공유 링크는 없음.
